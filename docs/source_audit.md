# Source Audit

Run:

```bash
npm run audit:data
```

The audit fails if generated records are missing provider, source URL, source name, retrieval timestamp, derived flag, methodology note, or data-quality status.
